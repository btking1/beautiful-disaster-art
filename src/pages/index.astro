---
import Layout from "../layouts/Layout.astro";
import ImgList from "../components/ImgList.jsx";
---

<Layout title="Beautiful Disaster Art">
  <main class="pt-2 px-2">
    <section class="py-2">
      <p>
        Welcome to <span>Beautiful Disaster</span> Art, where my art is <span
          >home</span
        >. I pour my <span>heart</span>
        and <span>soul</span> into each piece, capturing the <span>beauty</span>
        of the world. My goal is to <span>inspire</span> and bring <span
          >joy</span
        > to your life. <span>Thank you</span> for visiting and <span
          >supporting</span
        > me.
      </p>
    </section>

    <section id="img-list" class={`w-full ml-[-5%] pt-8`}>
      <ImgList amount={1} radius={false} client:idle />
    </section>
  </main>
</Layout>

<style>
  h1 {
    color: var(--accent-4);
    font-family: var(--inconsolata);
    font-weight: 400;
    font-size: var(--h1);
    text-shadow: 0 4px 4px rgba(0, 0, 0, 0.25);
  }

  h1 > span {
    color: var(--primary);
  }

  p {
    color: var(--accent-3);
    font-family: var(--athiti);
    font-size: var(--hero);
    font-style: normal;
    font-weight: 400;
    line-height: 29px;
    text-align: center;
    letter-spacing: 0.15em;
  }

  p > span {
    color: var(--primary-2);
  }
</style>
