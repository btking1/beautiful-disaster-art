---
import Layout from "../layouts/Layout.astro";
import ImgList from "../components/ImgList.astro";
---

<Layout title="Welcome to Astro.">
  <main class="flex flex-col items-start gap-11 py-3 px-2 mt-6 mb-20">
    <section>
      <p>
        Welcome to <span>Beautiful Disaster</span> Art, where my art is <span
          >home</span
        >. I pour my <span>heart</span>
        and <span>soul</span> into each piece, capturing the <span>beauty</span>
        of the world. My goal is to <span>inspire</span> and bring <span
          >joy</span
        > to your life. <span>Thank you</span> for visiting and <span
          >supporting</span
        > me.
      </p>
    </section>
    <section id="img-list" class={`flex flex-col w-full gap-y-3`}>
      <ImgList amount={1} />
    </section>
  </main>
</Layout>

<style>
  main {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    grid-gap: 1rem;
  }
  h1 {
    color: var(--accent-4);
    font-family: var(--inconsolata);
    font-weight: 400;
    font-size: var(--h1);
    text-shadow: 0 4px 4px rgba(0, 0, 0, 0.25);
  }

  h1 > span {
    color: var(--primary);
  }
  .slide-down {
    transform: translateY(0);
    transition: transform 0.3s ease-in-out;
  }

  #menu {
    transform: translateY(-100%);
    background-color: var(--accent-secondary-alpha);
    backdrop-filter: blur(0.2rem);
  }

  p {
    color: var(--accent-3);
    font-family: var(--athiti);
    font-size: var(--hero);
    font-style: normal;
    font-weight: 400;
    line-height: 29px;
    text-align: center;
    letter-spacing: 0.15em;
  }

  p > span {
    color: var(--primary-2);
  }
</style>
