---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Welcome to Astro.">
  <main class="flex flex-col items-start gap-11 px-3 py-3">
    <section>
      <p>
        Welcome to <span>Beautiful Disaster</span> Art, where my art is <span
          >home</span
        >. I pour my <span>heart</span>
        and <span>soul</span> into each piece, capturing the <span>beauty</span>
        of the world. My goal is to <span>inspire</span> and bring <span
          >joy</span
        > to your life. <span>Thank you</span> for visiting and <span
          >supporting</span
        > me.
      </p>
    </section>
    <section
      class="flex flex-col border border-solid border-red-400 w-full p-5 gap-3"
    >
      <img src="../public/assets/muva.jpg" alt="painting titled MUVA " />
      <img src="../public/assets/her.jpg" alt="painting titled her " />
      <img src="../public/assets/hearme.jpg" alt="painting titled hear me " />
      <img
        src="../public/assets/passion.jpg"
        alt="painting titled passion of heartbreak"
      />
    </section>
  </main>
</Layout>

<style>
  main {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    grid-gap: 1rem;
  }
  h1 {
    color: var(--accent-4);
    font-family: var(--inconsolata);
    font-weight: 400;
    font-size: var(--h1);
    text-shadow: 0 4px 4px rgba(0, 0, 0, 0.25);
  }

  h1 > span {
    color: var(--primary);
  }
  .slide-down {
    transform: translateY(0);
    transition: transform 0.3s ease-in-out;
  }

  #menu {
    transform: translateY(-100%);
    background-color: var(--accent-secondary-alpha);
    backdrop-filter: blur(0.2rem);
  }

  p {
    color: var(--accent-3);
    font-family: var(--athiti);
    font-size: var(--hero);
    font-style: normal;
    font-weight: 400;
    line-height: 29px;
    text-align: center;
    letter-spacing: 0.15em;
  }

  p > span {
    color: var(--primary-2);
  }

  img {
    border-radius: 5%;
    aspect-ratio: 1/1;
    width: 100%;
  }
  img:hover {
    transform: scale(1.1);
    transition: transform 0.3s ease-in-out;
  }
</style>
